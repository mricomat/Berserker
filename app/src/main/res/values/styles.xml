<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/red_border_button</item>
    </style>

    <style name="AppTheme.AppBarLayoutTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.ToolbarPopupTheme" parent="ThemeOverlay.AppCompat.Light" />
</resources>
